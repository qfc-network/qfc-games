{
  "name": "qfc-games-lobby",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite --port 3200",
    "build": "tsc && vite build",
    "preview": "vite preview --port 3200"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
